.span4
  = render partial: 'random_dog', locals: {dog: @presenter.random_dog}
.span8
  = render partial: 'shared/find_dog_mini_form'
  - if current_user 
    %h1= "Welcome, #{current_user.name}"
  - else
    %h1= "Welcome"
  %p
    this is a test 